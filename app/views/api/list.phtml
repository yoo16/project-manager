<? include('_header.phtml') ?>

<div class="pt-3">
  <?= PwForm::linkButton(['action' => 'new'], ['label' => LABEL_ADD, 'class' => 'btn btn-primary'] ) ?>
  <? include('views/components/lib/sortable.phtml') ?>

</div>

<table id="sortable-table" class="table">
  <thead class="thead-default">
    <tr>
      <th>&nbsp;</th>
      <th><?= LABEL_APIS_LABEL ?></th>
            <th><?= LABEL_APIS_NOTE ?></th>
            <th><?= LABEL_APIS_NAME ?></th>
          </tr>
  </thead>

  <?php if ($this->api->values): ?>
  <?php foreach ($this->api->values as $values): ?>
  <tr id="<?= $values['id'] ?>">
    <td><?= PwForm::linkButton(['action' => 'edit', 'id' => $values['id']], ['label' => LABEL_EDIT]) ?></td>
    <td><?= $values['label'] ?></td>
    <td><?= $values['note'] ?></td>
    <td><a href="<?= $this->urlFor(['controller' => 'root', 'id' => $values['id'], ['api_id' => $values['id']]) ?>"><?= $values['name'] ?></a></td>
  </tr>
  <?php endforeach ?>
  <?php endif ?>

</table>