
<div class="card">
  <div class="card-block">
  <?= nl2br($this->project->sql) ?>
  </div>
</div>
<table class="table">
  <?php foreach ($this->project->values as $project): ?>
  <tr>
    <td><?= $project['name'] ?></td>
    <td><?= $project['models_name'] ?></td>
    <td><?= $project['attributes_name'] ?></td>
  </tr>
<?php endforeach ?>
</table>

<table class="table">
  <tr>
    <?php foreach ($this->model->values as $model): ?>
    <td><?= $model['name'] ?></td>
  </tr>
<?php endforeach ?>
</table>
