<? if ($this->project): ?>

<div class="row mt-3 mb-3">

  <div class="col-sm-4">
    <div class="">
      <div class="card-block">
        <strong class="mr-1">Project</strong>
        <?= $this->project['name'] ?>
      </div>
    </div>
  </div>

  <div class="col-sm-4">
    <div class="mr-1">
      <div class="card-block">
        <strong class="">Database</strong>
        <?= $this->database['name'] ?>
      </div>
    </div>
  </div>

  <div class="col-sm-4">
    <ul class="nav justify-content-end">
      <li class="nav-item">
        <a class="btn btn-danger" href="#" data-toggle="modal" data-target="#php-export-window">PHP <?= LABEL_EXPORT ?></a>
        <a class="btn btn-danger" href="<?= url_for('export_db', $this->project['id']) ?>">DB <?= LABEL_EXPORT ?></a>
      </li>
    </ul>
  </div>

</div>

<? include('php_export_window.phtml') ?>

  <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
    <ul class="nav nav-pills">
      <li class="nav-item"><a class="nav-link <?= cssClass('page', $this->params['controller'], 'active'); ?>" href="<?= url_for('page/list', array('project_id' => $this->project['id'])) ?>">Page</a></li>
      <li class="nav-item"><a class="nav-link <?= cssClass('model', $this->params['controller'], 'active'); ?>" href="<?= url_for('model/list', array('project_id' => $this->project['id'])) ?>">Model</a></li>
    </ul>
  </nav>

<? endif ?>
