<table class="table">
  <thead class="thead-default">
    <tr>
      <th>&nbsp;</th>
      <th><?= DATABASE_NAME ?></th>
      <th><?= DATABASE_HOST ?></th>
      <th><?= DATABASE_USER ?></th>
      <th><?= DATABASE_PORT ?></th>
      <th>Delete Lock</th>
    </tr>
  </thead>

  <? if ($this->database->values): ?> 

  <? foreach ($this->database->values as $database): ?> 
  <tr>
    <td><a class="btn btn-outline-primary" href="<?= url_for('edit', $database['id']) ?>"><?= LABEL_EDIT ?></a></td>
    <td><a href="<?= url_for('tables', array('database_id' => $database['id'])) ?>"><?= $database['name'] ?></a></td>
    <td><?= $database['hostname'] ?></td>
    <td><?= $database['user_name'] ?></td>
    <td><?= $database['port'] ?></td>
    <td><?= FormHelper::activeLabelTag($database['is_lock']) ?></td>
  </tr>
  <? endforeach ?> 

  <? endif ?> 

</table>
