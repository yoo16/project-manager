<? include('_header.phtml') ?>

<div class="alert alert-success">
  <h2>Database</h2>
  <p>
    <dl>
      <dd>* If you want manage PostgreSQL on browser, please set the permission on pg_hba.conf, postgresql.conf. </dd>
      <dd>* project-manager need database '<?= DB_NAME ?>' by PostgreSQL 9.x. </dd>
    </dl>
  </p>
</div>

<div>
  <a class="btn btn-primary" href="<?= url_for('new') ?>"><?= LABEL_ADD ?></a>
  <a class="btn btn-outline-primary" href="#" data-toggle="modal" data-target="#import-table"><?= LABEL_IMPORT ?></a>
</div>

<div id="import-table" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">

      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Database Import</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body">
        <table class="table">
          <? foreach ($this->pg_databases as $pg_database): ?>
          <tr>
            <td><?= $pg_database['datname'] ?></td>
            <td>
              <form method="post" action="<?= url_for('import_database') ?>">
                <?= FormHelper::submit(LABEL_IMPORT) ?>
                <?= FormHelper::hidden('database_name', $pg_database['datname']) ?>
              </form>
            </td>
          </tr>
        <? endforeach ?>
      </table>

    </div>
  </div>
</div>
</div>

<? include('_list.phtml') ?>
