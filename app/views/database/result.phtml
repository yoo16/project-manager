<div class="hero-unit">

  <table class="table">
    <tr>
      <th>command</th>
      <td><?= $this->results['cmd'] ?></td>
    </tr>
    <tr>
      <th></th>
      <td>
    <? foreach ($this->results['output'] as $value): ?>
      <?= $value ?><br>
    <? endforeach ?>
      </td>
    </tr>
    <tr>
      <th>return</th>
      <td><?= $this->results['return'] ?></td>
    </tr>
  </table>

  <?= FormHelper::linkButton(['action' => 'index'], ['label' => LABEL_BACK]) ?>
</div>
