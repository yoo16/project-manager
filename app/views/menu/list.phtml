<? include('_header.phtml') ?>

<div class="pt-3">
  <?= PwForm::linkButton(['action' => 'new'], ['label' => LABEL_ADD, 'class' => 'btn btn-primary'] ) ?>
  <? include('views/components/lib/sortable.phtml') ?>

</div>

<table id="sortable-table" class="table">
  <thead class="thead-default">
    <tr>
      <th>&nbsp;</th>
      <th><?= LABEL_MENUS_IS_PROVIDE ?></th>
            <th><?= LABEL_MENUS_NAME ?></th>
          </tr>
  </thead>

  <? if ($this->menu->values): ?>
  <? foreach ($this->menu->values as $values): ?>
  <tr row-id="<?= $values['id'] ?>">
    <td><?= PwForm::linkButton('edit', $values['id'], ['label' => LABEL_EDIT]) ?></td>
    <td><?= PwForm::activeLabelTag($values['is_provide']) ?></td>
    <td><?= $values['name'] ?></td>
  </tr>
  <? endforeach ?>
  <? endif ?>

</table>