<? include('views/components/project_header.phtml') ?>
<? if ($this->page->value): ?>
  <h3><?= $this->page->value['label'] ?> : <?= $this->page->value['name'] ?></h3>

  <div>
    <?= $this->linkTo(['controller' => 'page_model'],
        [
        'label' => 'Model Requests',
        'class' => 'btn btn-outline-primary',
        'http_params' => ['page_id' => $this->page->value['id']]
        ]
        ) ?>
    <?= $this->linkTo(['controller' => 'view'],
        [
        'label' => 'View',
        'class' => 'btn btn-outline-primary',
        'http_params' => ['page_id' => $this->page->value['id']]
        ]
        ) ?>
  </div>
<? endif ?>