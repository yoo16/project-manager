<? include('_header.phtml') ?>

    <form action="<?= url_for('update', $this->params['id']) ?>" method="post">

        <table class="table">
            <tr>
            <th class="form-group row">
                ProjectPath
            </th>
            <td>
                <?= $this->user_project_setting->formInput('name', ['class' => 'form-control']) ?>
            </td>
            </tr>
        </table>

        <?= FormHelper::submit(LABEL_UPDATE) ?>
        <?= FormHelper::linkButton("cancel", null, ['label' => LABEL_BACK])?>
    </form>
    <?= $this->project->formDelete() ?>

    <form action="<?= url_for('update', $this->params['id']) ?>" method="post">

        <table class="table">
            <tr>
                <th>
                    <?= FormHelper::formLabel('Name', ['class' => 'col-2']) ?>
                </th>
                <td>
                    <?= $this->project->formInput('name', ['class' => 'form-control']) ?>
                </td>

                <th>
                    <?= FormHelper::formLabel('Database', ['class' => 'col-2']) ?>
                </th>
                <td>
                    <?= $this->project->formSelect('database_id', ['model' => 'Database', 'label' => 'name']) ?>
                </td>

            </tr>
        </table>

        <?= FormHelper::submit(LABEL_UPDATE) ?>
        <?= FormHelper::linkButton("cancel", null, ['label' => LABEL_BACK]) ?>
</form>