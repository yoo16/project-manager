<? include('_header.phtml') ?>

<div class="pt-3">
  <?= FormHelper::linkButton('new', null, ['label' => LABEL_ADD, 'class' => 'btn btn-primary']) ?>

  <? include('views/components/sortable.phtml') ?>
</div>

<table id="sortable-table" class="table table-list">
  <thead class="thead-default">
    <tr>
      <th>&nbsp;</th>
      <th><?= LABEL_RECORD_ITEMS_KEY ?></th>
      <th><?= LABEL_RECORD_ITEMS_VALUE ?></th>
      <th><?= LABEL_RECORD_ITEMS_VALUE_EN ?></th>
    </tr>
  </thead>

  <? if ($this->record_item->values): ?>
    <? foreach ($this->record_item->values as $values): ?>
      <tr id="<?= $values['id'] ?>">
        <td><?= FormHelper::linkButton('edit', $values['id'], ['label' => LABEL_EDIT, 'class' => 'btn btn-outline-primary']) ?></td>
        <td><?= $values['key'] ?></td>
        <td><?= $values['value'] ?></td>
        <td><?= $values['value_en'] ?></td>
      </tr>
    <? endforeach ?>
  <? endif ?>

</table>