<? include('_header.phtml') ?>

<div class="pt-3">
  <?= PwForm::linkButton(['action' => 'new'], ['label' => LABEL_ADD, 'class' => 'btn btn-primary']) ?>

  <? include('views/components/lib/sortable.phtml') ?>
</div>

<table id="sortable-table" class="table table-list">
  <thead class="thead-default">
    <tr>
      <th>&nbsp;</th>
      <th><?= LABEL_RECORD_ITEMS_KEY ?></th>
      <th><?= LABEL_RECORD_ITEMS_VALUE ?></th>
      <th><?= LABEL_RECORD_ITEMS_VALUE_EN ?></th>
    </tr>
  </thead>

  <?php if ($this->record_item->values): ?>
    <?php foreach ($this->record_item->values as $values): ?>
      <tr row-id="<?= $values['id'] ?>">
        <td><?= $this->record_item->linkEdit([], $values) ?></td>
        <td><?= $values['key'] ?></td>
        <td><?= $values['value'] ?></td>
        <td><?= $values['value_en'] ?></td>
      </tr>
    <?php endforeach ?>
  <?php endif ?>

</table>