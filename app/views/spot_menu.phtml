<? include('spot.phtml') ?>

<? if ($this->spot_menu->value['id']): ?>
  <h2>
    <?= $this->csv_options['menu_page_type'][$this->spot_menu->value['page_type']] ?>

    <? if ($this->spot_menu_instrument_type->value['id']): ?>
      -
      <?= $this->spot_menu_instrument_type->value['name'] ?>
    <? endif ?>
  </h2>
<? endif ?>