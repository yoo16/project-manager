<?= "<? include('_header.phtml') ?>" ?>

<div class="pt-3">
  <? Tag::init()->php('FormHelper::linkButton(\'new\', LABEL_ADD)') ?>

  <? Tag::init()->php('FormHelper::linkButton(\'sort_order\', LABEL_SORT_ORDER)') ?>

</div>

<table class="table">
  <thead class="thead-deffault">
    <tr>
      <th>&nbsp;</th>
<? foreach ($values['model']['attributes'] as $attribute): ?>
<? if (!Model::$required_columns[$attribute['name']]): ?>
      <th><?= $attribute['label'] ?></th>
<? endif ?>
<? endforeach ?>
    </tr>
  </thead>

  <? Tag::init()->ifs('$this->'.$values['model']['entity_name'].'->values') ?>
  <? Tag::init()->foreachs('$this->'.$values['model']['entity_name'].'->values as $values') ?>
  <tr>
    <td><? Tag::init()->php('FormHelper::linkButton(\'edit\', LABEL_EDIT, $values[\'id\'])') ?></td>
<? foreach ($values['model']['attributes'] as $attribute): ?>
<? if (!Model::$required_columns[$attribute['name']]): ?>
    <td><? Tag::init()->php('$values[\''.$attribute['name'].'\']') ?></td>
<? endif ?>
<? endforeach ?>
  </tr>
  <? Tag::init()->foreache() ?>
  <? Tag::init()->ife() ?>

</table>
