<?= "<? include('_header.phtml') ?>" ?>

<form action="<? Tag::init()->php('$this->urlSelfFor(\'add\')') ?>" method="post">
  <?= "<? include('form.phtml') ?>" ?>

  <div class="">
    <? Tag::init()->php('FormHelper::submit(LABEL_SAVE)') ?>

    <? Tag::init()->php('FormHelper::linkButton("cancel", null, [\'label\' => LABEL_BACK])') ?>

  </div>
</form>