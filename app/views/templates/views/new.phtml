<?= "<? include('_header.phtml') ?>" ?>

<form action="<? Tag::init()->php('$this->urlFor([\'action\' => \'add\'])') ?>" method="post">
  <?= "<? include('form.phtml') ?>" ?>

  <div class="">
    <? Tag::init()->php('PwForm::submit(LABEL_SAVE)') ?>

    <? Tag::init()->php('PwForm::linkButton([\'action\' => \'cancel\'], [\'label\' => LABEL_BACK])') ?>

  </div>
</form>