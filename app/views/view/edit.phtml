<? include('_header.phtml') ?>

<div class="card">
  <h3 class="card-header">View</h3>
  <div class="card-block">
    <form id="form-edit" action="<?= url_for('update', $this->view['id']) ?>" method="post" autocomplete="off">
      <div class="form-group row">
        <label for="view-name" class="col-1 col-form-label">Name</label>
        <input class="form-control col-5 <?= FormHelper::validateRequired($this->errors, 'name') ?>" type="text" name="view[name]" value="<?= $this->view['name'] ?>" required autocomplete="off">
      </div>

      <div class="form-group row">
        <label for="" class="col-1 col-form-label">Label</label>
        <input class="form-control col-5 <?= FormHelper::validateRequired($this->errors, 'label') ?>" type="text" name="view[label]" value="<?= $this->view['label'] ?>" required autocomplete="off">
      </div>
      
      <button class="btn btn-primary" type="submit"><?= LABEL_UPDATE ?></button>
      <a class="btn btn-outline-primary" href="<?= url_for('list') ?>"><?= LABEL_CANCEL ?></a>
    </form>

    <? if ($this->view['id']): ?>
    <form id="" action="<?=url_for('delete', $this->view['id']) ?>" method="post">
      <input class="btn btn-danger" type="submit" value="<?= LABEL_DELETE ?>">
    </form>
  <? endif ?>
</div>
</div>