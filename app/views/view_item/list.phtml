<? include('_header.phtml') ?>
<div class="pt-3">
  <?= FormHelper::linkButton('new', LABEL_ADD) ?>
  <?= FormHelper::linkButton('sort_order', LABEL_SORT_ORDER) ?>
  <?= FormHelper::linkButton('view/', LABEL_BACK) ?>
</div>

<table class="table table-sm">
  <thead class="thead-deffault">
    <tr>
      <th>&nbsp;</th>
      <th>カラム</th>
      <th>ラベル</th>
    </tr>
  </thead>

  <? if ($this->page->model->attribute->values): ?>
  <? foreach ($this->page->model->attribute->values as $values): ?>
  <tr>
    <td>
    <form action="<?= url_for('add_for_attribute') ?>" method="post">
    <?= FormHelper::submit(LABEL_ADD, ['class' => 'btn btn-sm btn-primary']) ?>
    <?= FormHelper::hidden('view_item[attribute_id]', $values['id']) ?>
    </form>
    </td>
    <td><?= $values['name'] ?></td>
    <td><?= $values['label'] ?></td>
  </tr>
  <? endforeach ?>
  <? endif ?>

</table>


<table class="table">
  <thead class="thead-deffault">
    <tr>
      <th>&nbsp;</th>
      <th>カラム</th>
      <th>ビュー</th>
      <th>ラベル</th>
      <th>Form</th>
      <th>class</th>
    </tr>
  </thead>

  <? if ($this->view->view_item->values): ?>
  <? foreach ($this->view->view_item->values as $values): ?>
  <tr>
    <td><?= FormHelper::linkButton('edit', LABEL_EDIT, $values['id']) ?></td>
    <td><?= FormHelper::activeLabelTag($values['attribute_id']) ?></td>
    <td><?= $this->view->value['name'] ?></td>
    <td><?= $values['label'] ?></td>
    <td><?= $values['form_type'] ?></td>
    <td><?= FormHelper::activeLabelTag($values['CSSクラス']) ?></td>
  </tr>
  <? endforeach ?>
  <? endif ?>

</table>
